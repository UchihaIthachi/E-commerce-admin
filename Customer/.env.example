# JWT Configuration
JWT_ACCESS_SECRET="your_jwt_access_secret_here_please_make_this_long_and_random"
JWT_REFRESH_SECRET="your_jwt_refresh_secret_here_please_make_this_long_and_random"

# Token expiry times (examples, adjust as needed)
# These can be in zeit/ms format (e.g., "15m", "7d", "1h") for jwt.sign
# or seconds for cookie maxAge. The code uses both, so provide appropriate values.
JWT_ACCESS_TOKEN_EXPIRES_IN="15m"
JWT_REFRESH_TOKEN_EXPIRES_IN="7d"

# Cookie maxAge in seconds (align with above expiry times)
# JWT_ACCESS_TOKEN_MAX_AGE=900 (15 * 60)
# JWT_REFRESH_TOKEN_MAX_AGE=604800 (7 * 24 * 60 * 60)

# Refresh token expiry in milliseconds (for direct Date calculations if needed)
# JWT_REFRESH_TOKEN_EXPIRES_IN_MS=604800000 (7 * 24 * 60 * 60 * 1000)


# Application URL (used for password reset links, OAuth redirect URIs etc.)
# Ensure this is the correct frontend URL that users will access.
NEXT_PUBLIC_APP_URL="http://localhost:3001" # Or your customer app's deployed URL


# Google OAuth Configuration
GOOGLE_CLIENT_ID="your_google_client_id_here"
GOOGLE_CLIENT_SECRET="your_google_client_secret_here"


# Prisma Database URL (already present in the project's .env, but good for reference)
# DATABASE_URL="postgresql://user:password@host:port/database?schema=public"
# DIRECT_URL="postgresql://user:password@host:port/database?schema=public" # If using Prisma Accelerate or Pulse or connection pooling

# Note: If you have specific values for MAX_AGE or EXPIRES_IN_MS in your .env file,
# the application code will use them. Otherwise, it falls back to defaults like '15m' or 7*24*60*60.
# It's good practice to define them explicitly if you need precise control.
# For example, if JWT_ACCESS_TOKEN_EXPIRES_IN="1h", then JWT_ACCESS_TOKEN_MAX_AGE should be 3600.
# If JWT_REFRESH_TOKEN_EXPIRES_IN="30d", then JWT_REFRESH_TOKEN_MAX_AGE should be 2592000.
# And JWT_REFRESH_TOKEN_EXPIRES_IN_MS should be 2592000000.

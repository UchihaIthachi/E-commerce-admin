# Prisma Database Configuration
# Replace with your actual database connection strings.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public&pgbouncer=true" # Example for PGBouncer, or if using Prisma Accelerate/Pulse

# Sanity CMS Configuration
NEXT_PUBLIC_SANITY_DATASET="your_sanity_dataset_name"
NEXT_PUBLIC_SANITY_PROJECT_ID="your_sanity_project_id"
SANITY_TOKEN="your_sanity_api_token" # Required for server-side Sanity requests or authenticated previews

# JWT Configuration
JWT_ACCESS_SECRET="your_jwt_access_secret_here_please_make_this_long_and_random_at_least_32_characters"
JWT_REFRESH_SECRET="your_jwt_refresh_secret_here_please_make_this_long_and_random_at_least_32_characters"

# Token expiry times
# These can be in zeit/ms format (e.g., "15m", "7d", "1h") for jwt.sign
# or seconds for cookie maxAge. The application code may use specific ones or fall back to defaults.
JWT_ACCESS_TOKEN_EXPIRES_IN="15m"    # How long the access token is valid
JWT_REFRESH_TOKEN_EXPIRES_IN="7d"   # How long the refresh token is valid

# If you need to set cookie maxAge directly in seconds (align with above):
# JWT_ACCESS_TOKEN_MAX_AGE=900      # 15 minutes * 60 seconds
# JWT_REFRESH_TOKEN_MAX_AGE=604800  # 7 days * 24 hours * 60 minutes * 60 seconds

# Application URL
# Used for generating absolute URLs (e.g., password reset links, OAuth redirect URIs).
# Ensure this is the correct public URL of your customer-facing application.
NEXT_PUBLIC_APP_URL="http://localhost:3000" # Use port 3000 for customer app default, or your deployed URL

# Google OAuth Configuration
# Get these credentials from Google Cloud Console.
GOOGLE_CLIENT_ID="your_google_client_id_here"
GOOGLE_CLIENT_SECRET="your_google_client_secret_here"

# Cloudflare R2 Storage (if used for image uploads or other assets by the Customer application)
# CLOUDFLARE_ACCESS_KEY_ID="your_cloudflare_r2_access_key_id"
# CLOUDFLARE_SECRET_ACCESS_KEY="your_cloudflare_r2_secret_access_key"
# CLOUDFLARE_ACCOUNT_ID="your_cloudflare_account_id"
# CLOUDFLARE_BUCKET_NAME="your_cloudflare_r2_bucket_name"
# CLOUDFLARE_PUBLIC_DOMAIN="your_cloudflare_r2_public_bucket_url" # e.g., mybucket.mydomain.com

# General Note:
# - For security, JWT secrets should be long, random, and kept confidential.
# - Ensure redirect URIs in Google Cloud Console match NEXT_PUBLIC_APP_URL + your callback path.
# - Database URLs should typically not be hardcoded here for production but sourced from environment variables.
#   This file serves as an example of what variables are needed.
